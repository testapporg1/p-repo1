pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Repository checked out successfully'
            }
        }

        stage('Polaris Fix PR Test') {
            steps {
                echo 'Testing Polaris Fix PR parameters'
            }
        }
    }
}
